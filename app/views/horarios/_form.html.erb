<%= form_for(@horario) do |f| %>
  <% if @horario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@horario.errors.count, "error") %> prohibited this horario from being saved:</h2>

      <ul>
      <% @horario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :horario_inicio %><br>
    <%= f.date_select :horario_inicio %>
  </div>
  <div class="field">
    <%= f.label :horario_fin %><br>
    <%= f.date_select :horario_fin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
